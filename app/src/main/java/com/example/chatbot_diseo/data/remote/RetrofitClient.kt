package com.example.chatbot_diseo.data.remote
/*
import retrofit2.Retrofit
import retrofit2.converter.gson.GsonConverterFactory

class RetrofitClient {

        // RECUERDA: 10.0.2.2 para emulador, o tu IP real para teléfono
        private const val BASE_URL = "http://192.168.1.17:7095/"

        private val retrofit: Retrofit by lazy {
            Retrofit.Builder()
                .baseUrl(BASE_URL)
                .addConverterFactory(GsonConverterFactory.create())
                .build()
        }

        val authApi: conversacionesAPIServices by lazy {
            retrofit.create(conversacionesAPIServices::class.java)
        }

    }
*/
